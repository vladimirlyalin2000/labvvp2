#include <iostream>
#include <math.h>
Using namespace std;
Int main()
{
	Setlocale(LC_ALL, “Russian”);
	Float x1, x2, x3, d1, d2, p;
	cout << "Enter A, B and C" << endl;
	cin >> x1 >> x2 >> x3;
	if (x1 < x3 < x2)
	{
	d1 = x3 - x1;
	d2 = abs(x3 - x2);
	p = d1 * d2;
	cout << "AC = " << d1 << endl << "BC = " << d2 << endl <<  "AC*BC = " << p;
	}
	Else
	{
	Cout << “C должна располагаться между точками A и B” << endl;
	}
}
